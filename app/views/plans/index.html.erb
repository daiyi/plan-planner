      <nav class="navbar navbar-fixed-top navbar-trip">
        <div class="container">
          <div class="navbar-header">
            <a class="navbar-brand" href="/">Trip Planning Planner</a>
          </div>
	  <ul class="nav navbar-nav navbar-right">
	    <li><button type="button" class="btn btn-primary btn-add-trip" data-toggle="modal" data-target="#addTripPlan">+ add trip plan</button></li>
	  </ul>
        </div>
      </nav>





      <div class="container">
      
        <p id="notice"><%= notice %></p>
      
        <div class="row">
	  <div id="grid">
            <% @plans.each do |plan| %>
	      <%= link_to(plan) do %>
	        <%= render partial: 'tile', locals: {plan: plan} %>
	      <% end %>
            <% end %>  
	  </div>
        </div>
        
        
        <div style="height:700px">
        </div>
        
      </div> <!-- /container -->



      <!-- modal -->

      <div class="modal fade" id="addTripPlan" tabindex="-1" role="dialog" aria-labelledby="addTripPlanLabel" aria-hidden="true">
	<div class="modal-dialog">
	  <div class="modal-content">
	    <div class="modal-header">
              <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
              <h4 class="modal-title" id="addTripPlanLabel">New Trip Plan</h4>
	    </div>
	    <%= render 'form_modal' %>
	  </div>
	</div>
      </div>


        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
	<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>     
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
